# Supabase (Python pipeline)
SUPABASE_URL=
# Optional alias fallback used by the Python loader when SUPABASE_URL is unset
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Mail (watchlist + inbox workflow)
EMAIL_ADDRESS=
EMAIL_PASSWORD=
SMTP_SERVER=
SMTP_PORT=587
IMAP_SERVER=
IMAP_PORT=993

# Webhook auth (Railway scraper API)
WEBHOOK_SECRET=
